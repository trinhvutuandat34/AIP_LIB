interface:
  display_name: "AIP LIB"
  short_description: "Repo-specific patterns and workflows for AIP_LIB"
  default_prompt: "Use the AIP_LIB repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true