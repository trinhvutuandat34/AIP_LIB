# 다중레이트 스케줄러 주기 (engine/match.py Match 기본값과 일치).
# 물리 dt = 1/120s. 정수 서브스텝으로 위상 정렬.
physics_hz: 120        # L4 JSBSim + L3 INDI (매 틱)
guidance_hz: 60        # L2 BFM 가이드 (÷2)
tactic_hz: 20          # L1 전술 BT (÷6)
log_hz: 30             # ACMI 프레임 기록
duration_s: 300        # 매치 최대 시간 (룰북 D1)
hard_deck_ft: 1000     # 이 고도 미만 = 즉시 패
# 그 외 판정: 실속(<100kts 누적 10s = 패), BT 예외 = DISQUALIFY, wall-clock 가드 = 무승부.
# Gun WEZ(50HP/s×각도계수, 500–3,000ft 균일, ATA 계단)는 aircombat/geometry/units.py WEZ_ATA_TIERS 참조.
